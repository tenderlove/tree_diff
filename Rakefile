# -*- ruby -*-

require 'rubygems'
require 'hoe'
require 'rubygems'
$: << File.expand_path(File.join(File.dirname(__FILE__), 'lib'))
require 'tree_diff'

Hoe.new('tree_diff', TreeDiff::VERSION) do |p|
  p.developer('Aaron Patterson', 'aaronp@rubyforge.org')
end

# vim: syntax=Ruby
